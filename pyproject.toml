[project]
name = "codeboost"
version = "0.1.0"
description = "Convert Python to high-performance C++ using Gemini"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "python-dotenv>=1.0",
    "gradio>=5.0",
]

[tool.uv]
package = false
